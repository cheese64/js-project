<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reviews</title>
</head>
<body>
    <% if(data.length){
        for(var i = 0; i < data.length; i++){ %>
        <p><%= data[i].professor %></p>
        <% }
        }else{ %>
        <p>No Reviews found</p>
        <% } %>
</body>
</html>